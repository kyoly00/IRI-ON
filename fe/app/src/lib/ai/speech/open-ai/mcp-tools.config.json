{
  "defaultVoiceTools": [
    {
      "type": "function",
      "name": "changeBrowserTheme",
      "description": "Changes the browser theme to light or dark mode.",
      "parameters": {
        "type": "object",
        "properties": {
          "theme": {
            "type": "string",
            "enum": [
              "light",
              "dark"
            ],
            "description": "The theme to apply."
          }
        },
        "required": [
          "theme"
        ]
      }
    },
    {
      "type": "function",
      "name": "endConversation",
      "description": "Ends the voice conversation when the user clearly wants to exit.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  ],
  "mcpTools": [
    {
      "type": "function",
      "name": "start_timer",
      "description": "Starts a timer for a specific cooking step. For example, call this function when the user says something like 'I need a 20-second timer for step 1.'",
      "parameters": {
        "type": "object",
        "properties": {
          "step": {
            "type": "integer",
            "description": "The cooking step number for which to start the timer."
          },
          "duration": {
            "type": "integer",
            "description": "Duration of the timer in seconds."
          },
          "message": {
            "type": "string",
            "description": "Optional message to deliver to the user when the timer finishes."
          }
        },
        "required": [
          "step",
          "duration"
        ]
      }
    },
    {
      "type": "function",
      "name": "send_video_url",
      "description": "Delivers the video URL for a specific cooking step. For example, call this function when the user says 'Show me the video for step 1.' The function will automatically match the current instruction text to the most relevant step.",
      "parameters": {
        "type": "object",
        "properties": {
          "step": {
            "type": "integer",
            "description": "The cooking step number (optional if text matching is used)."
          }
        },
        "required": []
      }
    },
    {
      "type": "function",
      "name": "web_search",
      "description": "Search the web for cooking-related information such as ingredient substitutes, where to buy ingredients, cooking tips, or recipe variations. Use this when the user asks about alternatives, replacements, purchasing options, or cooking advice. Examples: '대파 대체재료 뭐가 있어?', '이마트에서 어디서 살 수 있어?', '이 재료 없으면 뭐로 대체할 수 있어?'",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query. Should be specific and include context about what the user is looking for (ingredient name, substitute, store, etc.)."
          },
          "max_results": {
            "type": "integer",
            "description": "Maximum number of search results to return. Default is 5.",
            "default": 5
          }
        },
        "required": [
          "query"
        ]
      }
    },
    {
      "type": "function",
      "name": "open_coupang",
      "description": "Opens Coupang search page for ingredients when the user wants to buy something.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Target ingredient to search on Coupang."
          }
        },
        "required": [
          "query"
        ]
      }
    },
    {
      "type": "function",
      "name": "searchFoodNutrition",
      "description": "식약처 영양성분 DB를 검색합니다. Search for food nutrition information (calories, carbs, protein, fat, etc).",
      "parameters": {
        "type": "object",
        "properties": {
          "foodNameKr": {
            "type": "string",
            "description": "The name of the food to search for nutrition details (Korean)."
          },
          "makerName": {
            "type": "string",
            "description": "생산제조사명 (모를 경우 빈 문자열 '')"
          },
          "foodCategory1Name": {
            "type": "string",
            "description": "식품대분류명 (모를 경우 빈 문자열 '')"
          },
          "pageNo": {
            "type": "integer",
            "description": "Page number, defaults to 1."
          },
          "numOfRows": {
            "type": "integer",
            "description": "Number of rows to fetch, defaults to 5."
          }
        },
        "required": [
          "foodNameKr",
          "makerName",
          "foodCategory1Name"
        ]
      }
    }
  ],
  "mcpToolEndpoints": [
    {
      "id": "chef-tools.start_timer",
      "name": "start_timer",
      "description": "Timer start function",
      "endpoint": "/assistant/mcp/tools/start_timer",
      "method": "POST"
    },
    {
      "id": "chef-tools.send_video_url",
      "name": "send_video_url",
      "description": "Video URL delivery function",
      "endpoint": "/assistant/mcp/tools/send_video_url",
      "method": "POST"
    },
    {
      "id": "tavily.web_search",
      "name": "web_search",
      "description": "Web search using Tavily MCP",
      "endpoint": "/assistant/mcp/tools/web_search",
      "method": "POST"
    },
    {
      "id": "chef-tools.open_coupang",
      "name": "open_coupang",
      "description": "Coupang shopping tool",
      "endpoint": "/assistant/mcp/tools/open_coupang",
      "method": "POST"
    },
    {
      "id": "k-mfds-fooddb.searchFoodNutrition",
      "name": "searchFoodNutrition",
      "description": "Food nutrition DB search",
      "endpoint": "/assistant/mcp/tools/search_food_nutrition",
      "method": "POST"
    }
  ],
  "mcpServers": {
    "tavily-remote-mcp": {
      "command": "npx -y mcp-remote https://mcp.tavily.com/mcp/?tavilyApiKey=${TAVILY_API_KEY}",
      "env": {
        "TAVILY_API_KEY": "$TAVILY_API_KEY"
      }
    },
    "k-mfds-fooddb": {
      "command": "npx",
      "args": [
        "-y",
        "k-mfds-fooddb-mcp-server"
      ],
      "env": {
        "GOV_API_KEY": "$GOV_API_KEY"
      }
    }
  }
}